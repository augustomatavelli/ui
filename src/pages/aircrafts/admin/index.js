import MainCard from "components/MainCard";

// ==============================|| TAB - PERSONAL ||============================== //

const ListAircraftsForAdmin = () => {
	return <MainCard content={false} title="Aeronaves" sx={{ "& .MuiInputLabel-root": { fontSize: "0.875rem" } }}></MainCard>;
};

export default ListAircraftsForAdmin;
